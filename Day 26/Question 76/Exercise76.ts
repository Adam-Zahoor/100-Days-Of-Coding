// creating a function that will take two parameters, will add them together and return the final value 
function parametersAndReturn(num1: number, num2: number) {
    let finalValue: number = num1 + num2;
    console.log(finalValue);
};

// calling the function
parametersAndReturn(25, 70);