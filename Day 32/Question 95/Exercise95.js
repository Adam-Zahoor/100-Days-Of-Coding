"use strict";
// creating a function that will filter out the values that are greater than 10
function filteringNum(numbers) {
    console.log(numbers.filter(num => num > 10));
}
// calling the function with an array of numbers
filteringNum([44, 12, 8, 7, 8, 9, 76]);
