"use strict";
// // creating a function that will extract us a part of a string from the given parameter
// function extractingAString(extraction: string) {
//     console.log(extraction.substring(0, 10));
// };
// // calling the function with a string
// extractingAString("JavaScript and TypeScript both have almost the same syntax");
let myName = 'My name is Adam';
console.log(myName.substring(0, 6));
