// creating a function in which we assign an initial value and an updadted value to the variable 'myName'
function assigningAndUpdating() {
    let myName = "Adam";
    console.log(myName);
    myName = "Abrar";
    console.log(myName);
};

// calling the function
assigningAndUpdating();