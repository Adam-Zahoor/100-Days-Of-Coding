"use strict";
function addingStringsNumbers(Text, Info) {
    let completeSentence = `${Text}: ${Info}`;
    console.log(completeSentence);
}
;
addingStringsNumbers("Name", "Adam");
addingStringsNumbers("Age", 21);
