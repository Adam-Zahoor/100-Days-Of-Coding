"use strict";
// creating a function that will take string as a parameter and counts the numbers of characters present inside it
function numOfCharacters(string) {
    let length = string.length;
    console.log(length);
}
;
// calling the function with a string
numOfCharacters("My name is Adam");
