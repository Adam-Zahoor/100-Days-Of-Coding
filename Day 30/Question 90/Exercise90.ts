// creating a function that will take an input and check whether the input is a number or not
function checkingNaN(value: any) {
    console.log(isNaN(value));
};

// calling the function with a string and a number
checkingNaN("Adam");
checkingNaN(619);