// creating a function that will take a decimal number as a parameter and will round off it to an integer
function roundingOffInteger(decimal: number) {
    console.log(Math.round(decimal));
};

// calling the function two times with different values
roundingOffInteger(6.4355);
roundingOffInteger(7.7);